{{>navbar}}

<section>
    <div class="container pageedit">
        <p class="img"><img src="{{galerie.image}}" name="image"/></p>
    </div>
    {{#if admin}}
    <div class="control">
        {{!-- <a href="/galerie-edit/{{galerie._id}} {{image}}"><button type="button" class="btn">Editer</button></a> --}}
        <a href="/galerie-delete/{{galerie._id}}"><button type="button" name="subject" class="btn">supprimer</button></a>&emsp;
        <a href="/#galerie"><button type="button"  name="subject" class="btn">Retour</button></a>
       
        {{/if}}

    </div>
    </div>
</section>
{{>footer}}