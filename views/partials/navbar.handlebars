<!-- =======================================
                    Navbar
======================================== -->
<header id="top">
  <nav class="navbar navbar-expand-md navbar-light ">
    <div class="container-fluid">
      <div class="text-nav">
        <a href="/" class="navbar-brand">Bienvenue</a>
        <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
          <span class="navbar-toggler-icon"></span>
        </button><br>
        <div id="navbarCollapse" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <ul class="nav navbar-nav ml-auto">
              <li class="nav-item dropdown">

                {{#unless admin}}
                {{!-- <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Portfolio</a>
              <div class="dropdown-menu dropdown-menu">
                <a href="#" class="dropdown-item">Compétences</a>
                <a href="#" class="dropdown-item">Experience</a>
              </div> --}}
              </li>
              <li class="nav-item dropdown">
                <a href="#" class="nav-link  dropdown-toggle" data-toggle="dropdown">Présentation</a>
                <div class="dropdown-menu dropdown-menu">
                  <a href="https://syndicat-national-des-artistes-tatoueurs.assoconnect.com/page/86339-reglementation"
                    class="dropdown-item">Hygiène</a>
                  <a href="#" class="dropdown-item">Horaire / Tarif</a>
                  <a href="#" class="dropdown-item">Expérience</a>
                </div>
              </li>
              <li class="nav-item dropdown">
                <a href="/contact" class="nav-link">Contact</a>
              </li>
              {{/unless}}

              {{#if user}}
              {{#unless admin}}
              <li class="nav-item">
                <a class="nav-link" href="/#commentaire">Laisser un commentaire</a>
              </li>
              {{/unless}}

              {{#if admin}}
              <li class="nav-item">
                <a class="nav-link" href="/articles/add">Ajouter un événement</a>
              </li>

              <li class="nav-item">
                <a class="nav-link" href="/adminPage">Administration</a>
              </li>

              {{/if}}
              <li class="nav-item">
                <a class="nav-link" href="/logout">Déconnexion</a>
              </li>
              {{#unless admin}}
              <li class="nav-item">
               <a class="nav-link" href="/delete-user/{{sess.userId}}">Se désinscrire ?</a>
                {{!-- onclick="return confirm('Voulez-vous vraiment supprimer votre compte ?');" --}}
              </li>
              {{/unless}}
              {{else}}
              <li class="nav-item">
                <a class="nav-link" data-toggle="modal" data-target="#darkModalForm" href="#">Connexion</a>
              </li>

              <li class="nav-item">
                <a class="nav-link" data-toggle="modal" data-target="#darkModalForm2" href="#">Inscription</a>
              </li>

              {{/if}}
            </ul>
          </ul>
        </div>
      </div>
  </nav>
  </div>
  </div>
</header>
{{>script}}
